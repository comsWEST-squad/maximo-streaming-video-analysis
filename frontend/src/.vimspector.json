{
  "configurations": {
    "launch": {
      "adapter": "chrome",
      "configuration": {
        "stopOnEntry": true,
        "request": "launch",
        "url": "http://localhost:8080",
        "webRoot": "${workspaceRoot}"
      }
    }
  }
}
